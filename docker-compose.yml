services:
  iptv-player:
    build: .
    container_name: iptv-player
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - iptv-data:/app/data
    env_file:
      - .env

volumes:
  iptv-data:
